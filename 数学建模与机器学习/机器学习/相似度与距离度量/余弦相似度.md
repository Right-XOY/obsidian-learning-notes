# 核心定义

- 余弦相似度，计算**两个向量夹角的余弦值**，用来衡量两个向量方向有多接近。 只关心向量方向，不关心向量的长度（模长）
$$
\cos(\theta)=\frac{\boldsymbol a \cdot \boldsymbol b}{||\boldsymbol a||\;||\boldsymbol b||} =\frac{\sum_{i=1}^n a_i b_i}{\sqrt{\sum a_i^2}\sqrt{\sum b_i^2}}
$$
- 这里的 $||\boldsymbol a||$ 是向量 $a$ 的 L2范数，余弦相似度的取值范围是 [-1,1]
